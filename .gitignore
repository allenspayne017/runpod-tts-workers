# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Test/benchmark artifacts
*.wav
*.mp3
*.flac
*.ogg

# ...but DO commit the baked TADA reference voices (so voice_name works without a volume)
!tada-1b/voices/
!tada-1b/voices/*.wav

# Secrets / local env (never commit RunPod or cloud keys)
.env
*.key
*-firebase-adminsdk-*.json

# local watch log
gha_watch.log
