# Chatterbox Turbo worker EXTRAS only. chatterbox-tts + its deps are installed in the
# Dockerfile with --no-deps so it can't downgrade the base image's torch 2.7.1+cu128
# (required for Blackwell GPUs). torch/torchaudio come from the base image.
runpod
soundfile>=0.12
google-cloud-storage>=2.18.0
python-dotenv>=1.0.0
