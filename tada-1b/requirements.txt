# TADA-1B RunPod serverless worker deps.
# torch + torchaudio come from the Docker base image (do NOT re-pin — avoids CUDA conflicts).
runpod
numpy<2
hume-tada
soundfile>=0.12
google-cloud-storage>=2.18.0
python-dotenv>=1.0.0
huggingface_hub>=0.24
