# TADA-1B worker EXTRAS only. hume-tada is installed SEPARATELY (first) in the Dockerfile
# to avoid pip 'resolution-too-deep' — its transformers graph + google-cloud-storage together
# overwhelm pip's resolver. torch/torchvision/torchaudio come from the base image.
runpod
soundfile>=0.12
google-cloud-storage>=2.18.0
python-dotenv>=1.0.0
